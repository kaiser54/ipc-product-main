export default function ({ route, store, redirect }) {
      const welcome = store.state.welcomeFlow
      // console.log(welcome)
      // if (welcome === 'new-user') {
      //  store.commit('setWelcomeFlow', 'incomplete')
      // }
  }

  // to test the flow again, use "this.$store.reset()"